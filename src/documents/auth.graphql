fragment UserInfo on User {
  id
  username
  email
  roles
}

mutation login($request: LoginRequest!) {
  login(request: $request) {
    ...UserInfo
  }
}

mutation logout {
  logout
}

query user {
  user {
    ...UserInfo
  }
}
